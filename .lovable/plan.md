Add the provided Google Analytics 4 (gtag.js) script to `index.html`.

- Insert the async gtag loader script and the config/init script into the `<head>` section.
- Use the measurement ID provided: `G-BQ1HV47WKM`.

No other changes needed.
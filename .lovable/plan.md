Increase the footer logo size in `src/components/SiteFooter.tsx` by changing the image className from `h-16 w-auto` to `h-24 w-auto` (roughly 50% larger) so the wordmark reads more prominently above the about blurb.